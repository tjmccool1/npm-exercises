const niceMessage = ()  => {
    return ('This is a nice message for the lesson!');
};

const niceMessage2 = () => {
    return ('Hello, here is another nice message for the lesson!')};

module.exports = {niceMessage, niceMessage2};